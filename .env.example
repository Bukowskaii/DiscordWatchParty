# =============================================================================
# DiscordWatchParty — environment configuration (bot host only)
#
# Media server credentials are NOT configured here. After adding the bot to
# a Discord server, a server admin runs /setup configure to connect their
# Plex server (Jellyfin/Emby are experimental/untested). Credentials are
# stored per-server in data/guild_configs.db on this machine.
# =============================================================================


# ── Step 1: Discord ───────────────────────────────────────────────────────────
#
# 1. Go to https://discord.com/developers/applications and create an application.
# 2. Under Bot, create a bot and copy the token below.
# 3. Under General Information, copy the Application ID (your Client ID).
# 4. Invite the bot with the bot + applications.commands scopes AND the
#    "Manage Channels" permission (it creates a voice channel per watch party):
#    https://discord.com/api/oauth2/authorize?client_id=YOUR_CLIENT_ID&permissions=19472&scope=bot+applications.commands
# No privileged intents are required. Slash commands register automatically on
# startup — no manual deploy step needed.

DISCORD_BOT_TOKEN=
DISCORD_CLIENT_ID=

# 256-bit key used to encrypt guild API keys at rest in the SQLite database.
# Generate one with:  openssl rand -hex 32
# Keep this secret and backed up — losing it means existing configs can't be decrypted.
ENCRYPTION_KEY=


# ── Step 2: Public access ─────────────────────────────────────────────────────
#
# PUBLIC_URL is embedded in every watch link the bot posts. It must be the
# public https:// URL that reaches nginx — no trailing slash. How you expose
# nginx over HTTPS (reverse proxy, tunnel, etc.) is up to you; point it at
# http://<host>:<NGINX_PORT>.

PUBLIC_URL=https://watchparty.example.com

# Host port nginx listens on. Point your public proxy/tunnel here.
NGINX_PORT=8780


# ── Step 3: Optional tweaks ───────────────────────────────────────────────────

# How long a watch-session link stays valid (minutes). Default: 6 hours.
SESSION_TTL_MINUTES=360